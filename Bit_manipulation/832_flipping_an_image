class Solution:
    def flipAndInvertImage(self, image: List[List[int]]) -> List[List[int]]:
        ans=[]
        for row in image:
            ans.append([num^1 for num in row[::-1]])
        return ans
        